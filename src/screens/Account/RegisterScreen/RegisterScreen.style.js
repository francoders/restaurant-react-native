import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	img: {
		resizeMode: "contain",
		width: "100%",
		height: 140,
		marginTop: 30,
	},

	content: {
		width: 330,
	},

	contentAll: {
		justifyContent: "center",
		alignItems: "center",
	},
});
